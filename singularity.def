BootStrap: docker
From: python:3.10.11-slim-bullseye

%files
    ./requirements.txt /app/requirements.txt

%labels
	Author Redhawk18
	MyLabel textfooler

%post
	apt update
	apt install build-essential -y

	cd /app
	pip install -r ./requirements.txt

%runscript
	exec jupyter notebook --no-browser --port=8888 --ip=0.0.0.0

